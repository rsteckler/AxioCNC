# Workflow for building and deploying website (static + Docusaurus docs) to GitHub Pages
name: Deploy Website

on:
  push:
    branches: ["main"]
    paths:
      - 'website/**'
      - '.github/workflows/website.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'yarn'

      - name: Install dependencies
        run: yarn install --immutable

      - name: Build Docusaurus
        working-directory: website/docs
        env:
          GISCUS_REPO: ${{ secrets.GISCUS_REPO || 'rsteckler/AxioCNC-Docs' }}
          GISCUS_REPO_ID: ${{ secrets.GISCUS_REPO_ID }}
          GISCUS_CATEGORY: ${{ secrets.GISCUS_CATEGORY || 'Announcements' }}
          GISCUS_CATEGORY_ID: ${{ secrets.GISCUS_CATEGORY_ID }}
        run: yarn build

      - name: Debug build output
        run: |
          echo "=== Build directory structure ==="
          ls -la website/docs/build/ || echo "Build directory not found"
          echo ""
          echo "=== Top-level files in build ==="
          find website/docs/build -maxdepth 1 -type f | head -20
          echo ""
          echo "=== Directories in build ==="
          find website/docs/build -maxdepth 1 -type d
          echo ""
          echo "=== Looking for index.html ==="
          find website/docs/build -name "index.html" -type f
          echo ""
          echo "=== Checking for docs subdirectory ==="
          ls -la website/docs/build/docs/ 2>/dev/null || echo "No docs/ subdirectory found"

      - name: Prepare deployment
        run: |
          mkdir -p _site/docs
          # Copy static website files to root
          cp -r website/static/* _site/ 2>/dev/null || true
          # Copy Docusaurus build output (with baseUrl='/docs/', files are in build/ directly)
          if [ -d "website/docs/build" ]; then
            cp -r website/docs/build/* _site/docs/
          fi
          # Ensure .nojekyll for GitHub Pages
          touch _site/.nojekyll _site/docs/.nojekyll

      - name: Debug deployment structure
        run: |
          echo "=== _site directory structure ==="
          ls -la _site/ || echo "_site directory not found"
          echo ""
          echo "=== _site/docs directory structure ==="
          ls -la _site/docs/ || echo "_site/docs directory not found"
          echo ""
          echo "=== Looking for index.html in _site ==="
          find _site -name "index.html" -type f
          echo ""
          echo "=== Top-level files in _site ==="
          find _site -maxdepth 1 -type f | head -20
          echo ""
          echo "=== Files in _site/docs ==="
          find _site/docs -maxdepth 1 -type f | head -20

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '_site'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
